# Changelog

## 0.1.0
- Primera versión. Comandos "Convertir selección/archivo a PySpark" que llaman
  al endpoint `/py2spark` del portal BNX (pandas → PySpark 3). Configurable
  `py2spark.serverUrl` y `py2spark.openInNewEditor`.
